# 遥感数据质量审查与场景人工精选工具：代码生成指导 1.0

## 1. 项目定位

本项目用于对不同来源的遥感图像-mask数据集进行人工质量审查、场景选择、进度记录、统计和分类导出。

它是一个独立的本地桌面工具，后续代码统一放在：

```text
D:/硕士毕业论文/scene_review_tool
```

工具不修改、不重命名、不移动原始图像和 mask。导入阶段只建立索引；人工选择结果独立保存在项目工作区；只有用户主动执行“导出”时，才将选中的数据复制或链接到新目录。

本工具的核心价值不是自动决定场景，而是为人工精选 benchmark 测试样本提供高效、可恢复、可追溯的操作界面。

## 2. 首版技术路线

首版采用：

```text
Python 3.11+
PySide6
SQLite
Pillow + NumPy
rasterio（TIFF/GeoTIFF/高位深遥感图像）
OpenCV（必要的颜色转换和缩放）
```

选择 PySide6 桌面应用的原因：

- 数据均在本机，桌面应用可以直接访问文件路径，不需要上传图像。
- 适合大量连续审图、键盘快捷键、后台预加载和本地缓存。
- SQLite 可持续保存进度，关闭程序或异常退出后可以继续。
- 对 TIFF、GeoTIFF、高位深和大尺寸图像的兼容性优于简单网页上传方案。
- UI 可定制为稳定的三栏审查界面，避免频繁刷新整页。

首版不使用 Electron，也不将 Streamlit 作为主实现。Streamlit 可以快速演示，但在大量图像连续切换、键盘审查、缓存控制和精细 UI 状态管理方面不适合作为最终工具。

## 3. 设计原则

1. **原始数据只读**：所有导入操作只生成索引，不改变原始文件。
2. **适配不同数据集**：图像目录、mask目录、文件名规则和类别规则均可配置。
3. **自动保存**：每次质量决定、场景选择和备注修改后立即写入 SQLite。
4. **避免重复审查**：样本有稳定 ID，已审样本在列表中明确标记，默认跳到下一条未审样本。
5. **场景词表统一与自定义并存**：优先读取现有 `geo_scene_taxonomy_1.0.json` 作为正式场景；允许保留用户自定义场景，但自定义场景必须单独标记，不能静默混入正式体系。
6. **人工结论优先**：模型预测只能作为可选候选，不得覆盖人工选择。
7. **高响应速度**：索引、缩略图、图像解码和 mask 叠加均采用懒加载、缓存和后台线程。
8. **可追溯导出**：任何导出样本都能追溯到原始图像、原始 mask、人工决定和导出时间。

## 4. 功能范围

### 4.1 数据集导入

用户创建一个审查项目时填写：

- 数据集名称。
- 图像文件夹。
- mask 文件夹。
- 可选的父级图像或上下文图文件夹。
- 输出工作区。
- 图像与 mask 的配对规则。
- mask 类型和类别配置。
- 场景体系 JSON 路径。

导入后先进入“配对预检”，显示：

- 找到的图像数量。
- 成功匹配的 image-mask 数量。
- 缺少 mask 的图像。
- 找不到图像的孤立 mask。
- 重复文件名或一对多冲突。
- 无法读取或尺寸不一致的文件。

只有用户确认预检结果后才建立正式项目索引。

### 4.2 图像与 mask 配对规则

不同数据集的文件组织不同，首版必须支持以下规则：

1. **同名 stem**：`images/a.png` 对应 `masks/a.png`。
2. **固定前后缀**：`a_image.tif` 对应 `a_mask.png`。
3. **正则替换**：通过正则表达式把图像名转换为 mask 名。
4. **相对目录镜像**：图像和 mask 保持相同子目录结构。
5. **映射表导入**：读取 CSV 中的 `image_path, mask_path`。

配对规则保存在数据集配置中，不能硬编码到 UI 或主程序。

建议配置示例：

```json
{
  "dataset_name": "CDUWD1.0-512",
  "image_root": "D:/.../CDUWD1.0/512",
  "mask_root": "D:/.../CDUWD1.0/512",
  "pairing": {
    "mode": "mirrored_subdirectories",
    "image_dir_name": "images",
    "mask_dir_name": "labels",
    "allowed_image_extensions": [".png", ".jpg", ".jpeg", ".tif", ".tiff"],
    "allowed_mask_extensions": [".png", ".tif", ".tiff"]
  },
  "mask": {
    "type": "binary",
    "background_values": [0],
    "foreground_values": [1, 255]
  }
}
```

### 4.3 图像格式兼容

首版至少支持：

```text
PNG, JPG, JPEG, BMP, WEBP, TIF, TIFF, GeoTIFF
```

扩展支持：

```text
JP2, NPY
```

读取策略：

- 普通 8-bit 图像优先使用 Pillow。
- TIFF、GeoTIFF、多波段和 16-bit 图像使用 rasterio。
- 单波段图像默认使用百分位拉伸显示。
- 三波段及以上图像允许选择 RGB 波段组合。
- 显示变换只影响预览，不改变原始像素。
- 读取失败不能使整个项目崩溃，应将样本标记为 `read_error` 并允许跳过。

### 4.4 图像与 mask 显示

主显示区必须支持：

- 显示原图。
- 通过勾选框显示或隐藏 mask。
- 使用滑块调整 mask 透明度，范围 0% 至 100%。
- 二值 mask 使用醒目的单一颜色。
- 多类别 mask 使用固定调色板和图例。
- 可选择只显示某个 mask 类别或显示全部类别。
- 原图、叠加图和 mask-only 三种视图快速切换。
- 鼠标滚轮缩放。
- 鼠标拖拽平移。
- 双击恢复“适合窗口”。
- 显示鼠标位置像素坐标和当前 mask 值。
- 可选显示图像尺寸、数据类型、波段数和文件路径。

mask 与图像尺寸不一致时：

- 默认禁止直接叠加。
- UI 显示明确错误。
- 只有配置文件明确规定最近邻缩放规则时，才允许生成临时预览。
- 临时预览不得覆盖原始 mask。

### 4.5 质量检查

质量结论与场景选择必须分开记录。质量状态采用：

```text
unreviewed：尚未检查
accepted：图像与 mask 可用
needs_correction：mask 或配对需要修正
rejected：不适合作为测试样本
```

可选质量问题标签：

```text
mask misalignment
incomplete mask
wrong label
empty mask
tiny noisy target
blurred image
cloud or shadow
duplicate sample
insufficient context
read error
other
```

质量问题标签用于筛选和统计，人工说明保存在自由文本备注中。

### 4.6 场景人工选择

场景选择采用“正式体系优先 + 当前数据集常用场景优先 + 自定义场景可保留”的方式。UI 从以下文件读取三级体系：

```text
D:/硕士毕业论文/ppt相关/geo_scene_taxonomy_1.0.json
```

选择方式：

1. 默认先显示“当前数据集已使用场景”，即本项目中已经被人工选择过的主要场景，按使用次数和最近使用时间排序。
2. 如果当前数据集常用场景中没有合适项，再进入“从 1.0 场景体系选择”，先选一级地理环境域，再选该一级下的合法二级场景。
3. 如果 1.0 场景体系中仍没有合适项，允许输入“自定义场景”。
4. 选择一个主要场景，可选添加多个次要场景。
5. 可选填写人工备注，解释入选、不确定或自定义原因。

场景来源类型采用：

```text
taxonomy：来自 1.0 场景体系
dataset_custom：当前数据集自定义场景
candidate_new_taxonomy：可能需要后续纳入场景体系的新场景
```

其中：

- `taxonomy` 是后续 benchmark 初版最优先使用的规范场景。
- `dataset_custom` 可以用于当前数据集整理、导出和统计，但不能直接当作正式体系场景写入论文。
- `candidate_new_taxonomy` 用于记录人工发现的体系缺口，后续统一讨论是否升级为 1.1 场景体系。

自定义场景输入规则：

- 必须填写英文规范名和中文说明。
- 英文名建议使用自然英文短语，如 `urban residential area`，避免随意缩写。
- 系统自动检查是否与已有二级场景同义或近义，并提示可映射到的正式场景。
- 自定义场景不能覆盖已有正式场景的名称。
- 自定义场景只在当前审查项目中生效，除非后续人工导出为 taxonomy 更新建议。

数据库中同时保存：

- 一级 ID 和规范名称。
- 二级规范英文词。
- 场景来源类型。
- 自定义场景英文名和中文说明。
- 可选的正式体系映射建议。
- 人工备注文本。
- 场景体系版本和文件哈希。

场景状态采用：

```text
unassigned：未选择
assigned：已选择
uncertain：场景不确定，需要再次复核
```

对于 512 tile 场景不清楚的情况，UI 应支持打开父级 1024 图或更大上下文。人工场景应依据 tile 与父级上下文共同判断，但正式测试输入仍由项目配置决定。

#### 4.6.1 当前数据集常用场景选择器

为了提高连续审图速度，右侧审查面板的场景选择不应让用户每次都从几十个二级场景中查找。首版应实现一个“当前数据集常用场景”选择器：

- 显示本项目中已经使用过的主要场景。
- 每个场景显示中文名、英文名、来源类型和已使用数量。
- 最近使用的场景排在前面，高频场景排在前面。
- 支持搜索正式场景和自定义场景。
- 支持一键选择上一次使用的场景。
- 支持将若干场景固定到顶部，适合一个数据集只审查少数几个候选场景的情况。

推荐交互顺序：

```text
当前数据集常用场景
  -> 搜索 1.0 场景体系
  -> 添加自定义场景
```

这样既能保证操作效率，也能避免大量自由输入导致同义不同名。

### 4.7 已处理样本的区分

不能通过移动原图来表示进度。工作区通过数据库和 UI 状态区分：

| 状态 | 列表表现 |
|---|---|
| 未检查 | 灰色空心状态点 |
| 已检查且已分场景 | 绿色状态点和场景名称 |
| 已检查但未分场景 | 黄色状态点 |
| 需要修正或复核 | 橙色状态点 |
| 已拒绝 | 红色状态点和删除线样式 |
| 读取错误 | 紫红色错误图标 |

工具提供：

- 默认跳转到下一张未检查图像。
- 仅查看未检查、已接受、待修正、已拒绝或场景未分配样本。
- 按一级场景、二级场景、质量问题和数据子集过滤。
- 按文件名、样本 ID 和备注搜索。
- 显示总进度和当前过滤结果进度。

## 5. UI 设计

### 5.1 整体布局

主窗口采用稳定的三栏布局：

```text
┌─────────────────────────────────────────────────────────────────┐
│ 项目/数据集  搜索  过滤器                   已审 328 / 950      │
├──────────────┬──────────────────────────────┬───────────────────┤
│ 样本列表      │ 图像预览                     │ 人工审查面板       │
│ 缩略图        │ 原图 + mask overlay          │ 质量状态           │
│ 状态点        │ 缩放、平移、透明度            │ 一级/二级场景       │
│ 文件名        │ mask显示开关、类别图例         │ 次要场景、问题标签  │
│              │                              │ 备注               │
├──────────────┴──────────────────────────────┴───────────────────┤
│ 上一张  下一张  保存状态  父级上下文  当前路径/尺寸/加载状态     │
└─────────────────────────────────────────────────────────────────┘
```

布局要求：

- 中央图像预览始终占据最大空间。
- 左侧列表宽度可调整，支持只显示文件名或缩略图。
- 右侧审查面板使用紧凑表单，不使用多层嵌套卡片。
- 颜色用于状态和 mask，不使用大面积渐变或装饰性背景。
- 卡片圆角不超过 8 px，主要使用清晰分区和细边框。
- 深色与浅色主题至少完成一种，首版推荐中性深灰工作界面，使彩色 mask 更醒目。
- 所有按钮使用明确图标和工具提示；常用命令同时支持快捷键。

### 5.2 建议快捷键

```text
Left / A       上一张
Right / D      下一张
Space          显示/隐藏 mask
1              accepted
2              needs_correction
3              rejected
U              scene uncertain
F              图像适合窗口
Ctrl+F         搜索样本
Ctrl+S         强制保存
Ctrl+E         打开导出页
```

场景选择不建议直接绑定数字键，因为场景数量多且会随 taxonomy 调整。

### 5.3 辅助视图

除主审查页外，首版需要：

- **项目创建页**：选择目录、配对规则和场景体系。
- **导入预检页**：检查匹配、重复、尺寸和读取错误。
- **统计页**：查看质量状态、场景分布和未完成数量。
- **导出页**：选择导出范围、文件模式和目标目录。

不制作宣传式首页。程序启动后直接显示最近项目或项目选择器。

## 6. 数据持久化

### 6.1 工作区结构

每个审查项目拥有独立工作区：

```text
workspace/
  project.json
  review.sqlite3
  cache/
    thumbnails/
    previews/
  exports/
  logs/
```

`project.json` 保存数据集配置和场景体系路径；`review.sqlite3` 保存索引与人工结果；缓存可以安全删除后重新生成。

### 6.2 样本稳定标识

每个样本使用以下信息生成稳定 ID：

```text
dataset_id + normalized_relative_image_path + image_file_size
```

另外可异步计算图像 SHA-256，用于发现跨目录重复内容。不能只用文件名作为 ID，因为不同子目录可能存在同名图像。

### 6.3 最小数据库表

#### datasets

```text
id
name
image_root
mask_root
context_root
config_json
taxonomy_version
taxonomy_sha256
created_at
```

#### samples

```text
id
dataset_id
image_path
mask_path
context_path
parent_group_id
image_width
image_height
image_dtype
mask_width
mask_height
read_status
content_sha256
```

#### reviews

```text
sample_id
quality_status
scene_status
level1_id
level1_name
primary_level2_scene
secondary_scenes_json
scene_source
custom_scene_name_en
custom_scene_name_zh
taxonomy_mapping_suggestion
issue_tags_json
reviewer_note
reviewed_at
updated_at
```

#### history

```text
id
sample_id
changed_field
old_value
new_value
changed_at
```

`history` 用于撤销误操作和追踪人工修改，不保存图像副本。

#### dataset_scene_vocab

```text
id
dataset_id
scene_source
scene_name_en
scene_name_zh
level1_id
level1_name
mapped_level2_scene
is_pinned
use_count
last_used_at
created_at
```

`dataset_scene_vocab` 保存当前数据集已经使用过或固定的场景。它不是正式场景体系文件，而是为了提高人工审查效率。正式体系仍以 `geo_scene_taxonomy_1.0.json` 为准。

## 7. 性能要求

性能是必要条件，首版实现必须遵循：

1. 导入时只扫描路径和基础元数据，不一次性解码全部原图。
2. 图像读取、缩略图生成和 SHA-256 计算放入 `QThreadPool` 后台线程。
3. 当前样本显示后，后台预取前后各 2 至 5 张图像。
4. 使用 LRU 内存缓存保存最近解码图像和 mask。
5. 大尺寸 TIFF 使用 rasterio 的窗口读取或降采样读取，避免为预览加载全部分辨率。
6. 缩略图缓存到磁盘，并以文件修改时间和尺寸判断是否失效。
7. mask 叠加只在显示分辨率上计算，不在每次透明度变化时重读原文件。
8. 列表采用 Qt Model/View 和虚拟化加载，不为数千样本一次创建数千个复杂控件。
9. SQLite 使用事务和 WAL 模式；单条人工操作立即提交，但批量索引采用批事务。
10. 任何后台任务都不得阻塞主 UI 线程。

首版性能验收目标：

- 已建立缓存后，切换普通 512/1024 图像的可见响应目标小于 200 ms。
- 首次读取大 TIFF 时显示加载状态，UI 保持可操作。
- 1 万条样本列表滚动无明显卡顿。
- 异常退出后，最近一次已确认操作不丢失。

## 8. 统计功能

统计页至少显示：

- 总样本数。
- 成功匹配、缺少 mask、读取错误数量。
- 未检查、已接受、待修正、已拒绝数量。
- 已分场景、未分场景、场景不确定数量。
- 各一级场景数量。
- 各二级场景数量。
- 每个二级场景下的质量状态分布。
- 每个父级区域或数据子集的审查进度。

支持将当前统计导出为：

```text
review_summary.csv
scene_statistics.csv
unreviewed_samples.csv
rejected_samples.csv
review_manifest.jsonl
```

CSV 使用 UTF-8 BOM，确保 Windows Excel 直接打开不乱码。

## 9. 导出模式

### 9.1 导出范围

用户可以选择：

- 只导出 `accepted + assigned`。
- 导出所有已分场景样本。
- 导出当前过滤结果。
- 是否包含 `uncertain`。
- 是否将未划分样本输出到 `_unassigned`。
- 是否同时导出父级上下文图。

### 9.2 文件写入方式

支持：

```text
copy：复制文件，默认且最兼容
hardlink：同一磁盘时节省空间
symlink：高级选项，需要系统权限
manifest_only：只导出清单，不复制图像
```

原始文件永远不移动。

### 9.3 导出目录结构

默认使用主要二级场景的规范英文名称作为文件夹：

```text
export_20260818_153000/
  river/
    images/
    masks/
    contexts/
    manifest.csv
  canal/
    images/
    masks/
    contexts/
    manifest.csv
  urban green space/
    images/
    masks/
    contexts/
    manifest.csv
  _unassigned/
  summary/
    scene_statistics.csv
    unreviewed_samples.csv
    export_manifest.jsonl
    export_config.json
```

若出现重名文件，导出名使用 `sample_id` 前缀，不覆盖已有文件。

导出前显示预计文件数量和空间占用；导出过程支持取消；重复导出不能静默覆盖旧目录，应创建带时间戳的新目录或要求用户确认。

## 10. 与场景体系和 benchmark_builder 的关系

本工具与现有项目关系如下：

```text
geo_scene_taxonomy_1.0.json
           │
           ├── benchmark_builder：自动候选、属性统计、初步筛选
           │
           └── scene_review_tool：人工质量检查、场景确认、样本精选
                                      │
                                      └── 导出高质量场景子集
```

首版不要求 `benchmark_builder` 才能运行。用户可以直接选择任意图像和 mask 文件夹创建项目。

后续可选扩展：

- 读取 `benchmark_builder` 的 RemoteCLIP Top-K 候选，显示在右侧作为参考。
- 读取 mask 面积比例、连通域数量和父图关系。
- 将人工确认结果导回 `benchmark_builder`，用于 benchmark 样本生成。

模型候选与人工结论必须分字段保存，不能用人工选择覆盖模型原始输出，也不能让模型重新运行时覆盖人工记录。

## 11. 建议代码结构

```text
scene_review_tool/
  代码生成指导_1.0.md
  README.md
  pyproject.toml
  src/
    scene_review_tool/
      __init__.py
      __main__.py
      app.py
      domain/
        models.py
        enums.py
      adapters/
        base.py
        directory_pair.py
        csv_mapping.py
        registry.py
      imaging/
        reader.py
        mask_reader.py
        compositor.py
        thumbnail_cache.py
      taxonomy/
        loader.py
      storage/
        database.py
        migrations.py
        repositories.py
      services/
        import_service.py
        review_service.py
        statistics_service.py
        export_service.py
      ui/
        main_window.py
        models/
          sample_list_model.py
        pages/
          project_page.py
          import_preview_page.py
          review_page.py
          statistics_page.py
          export_page.py
        widgets/
          image_viewer.py
          mask_controls.py
          scene_selector.py
          review_panel.py
  tests/
    test_pairing.py
    test_taxonomy.py
    test_database.py
    test_export.py
    test_mask_overlay.py
  configs/
    examples/
      same_stem_binary_mask.json
      mirrored_multiclass_mask.json
      cduwd_512.json
```

## 12. 分阶段开发顺序

### 阶段 1：最小可用版本

- 创建项目。
- 同名或镜像目录 image-mask配对。
- 图像和二值 mask 叠加。
- mask 显示开关和透明度。
- 上一张/下一张。
- 质量状态、一级/二级场景和备注。
- SQLite 自动保存。
- 已审/未审过滤。
- CSV/JSONL 统计导出。
- 按场景复制到新目录。

### 阶段 2：数据兼容和性能

- 正则、前后缀和 CSV 配对规则。
- TIFF、GeoTIFF、高位深和多波段显示。
- 多类别 mask 和图例。
- 后台加载、磁盘缩略图和 LRU 缓存。
- 父级上下文图。
- 重复内容检测。

### 阶段 3：benchmark协同

- 读取 `benchmark_builder` 场景候选和属性。
- 按 mask 面积、形态和父级区域过滤。
- 批量设置场景或质量标签，但每次批量操作必须二次确认。
- 将人工精选结果导入后续文本-mask任务生成流程。

## 13. 首版验收场景

首版至少通过以下测试：

1. 导入一个同名 PNG image-mask 数据集，匹配数量正确。
2. 导入 CDUWD 512 镜像目录结构，能够识别六个数据子组。
3. 选择任意样本，原图和 mask 正确对齐。
4. mask 开关和透明度调整不重新读取文件，交互流畅。
5. 为样本选择 `水系与湿地 -> river`，关闭并重启后结果仍存在。
6. 已使用过的场景会出现在“当前数据集常用场景”中，可以一键复用。
7. 输入自定义场景后，能够保存来源类型、英文名、中文说明和可选映射建议。
8. 已处理样本在列表中有明显状态，默认可以跳到下一张未处理图像。
9. 过滤未划分样本时数量与统计页一致。
10. 导出 `river` 后，图像和 mask 均进入对应目录，原文件位置不变。
11. 导出清单能够追溯原始绝对路径、场景、质量状态和时间。
12. CSV 用 Excel 打开中文不乱码。
13. 图像和 mask 尺寸不一致时明确报错，不产生错误叠加。
14. 同一图像重复导入时能够识别已有记录或要求创建新项目，不静默重复审查。

## 14. 首版暂不实现

为保证项目小而稳定，首版暂不实现：

- 在 UI 内直接编辑 mask 像素。
- 自动训练或微调场景模型。
- 多人在线协作和账号系统。
- 云端存储和远程部署。
- 自动移动或删除原始文件。
- 将自由文本直接作为新的正式场景词；自定义场景必须以 `dataset_custom` 或 `candidate_new_taxonomy` 单独保存。

如后续需要修正 mask，应先记录 `needs_correction`，再由独立标注工具处理，避免本工具演变为复杂的全功能标注平台。

## 15. 最终实现目标

最终用户操作应简化为：

```text
选择图像和 mask 文件夹
  -> 检查配对结果
  -> 逐图查看原图和 mask叠加
  -> 判断质量
  -> 从场景体系选择主场景
  -> 自动保存并跳到下一张
  -> 查看进度和场景统计
  -> 按场景导出高质量样本
```

工具必须始终做到：原始数据不动、进度不丢、场景词统一、审查可追溯、界面响应流畅。
